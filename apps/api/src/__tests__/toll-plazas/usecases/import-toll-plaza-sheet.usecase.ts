import { it, describe, expect } from '@jest/globals';
// import { importTollPlazaSheet, ImportTollPlazaSheetDto } from '~/toll-plazas/usecases';
// mock('~/shared/prismma');

describe('Import toll plaza sheet - Use case', () => {
  it('Should import', () => {
    expect('ola').toBe('asd');
  });
  // it('Should import', () => {
  //   const dto: ImportTollPlazaSheetDto = {
  //     sheetPath: '',
  //     startAt: new Date(),
  //     endAt: new Date(),
  //     description: '',
  //   };
  //   expect(importTollPlazaSheet(dto)).rejects.toBe(false);
  // });
});
